package abstract_factory_example.toasts;

/**
 * Produto abstrato da família Toast.
 * Define a interface comum para os toasts que serão criados pelas fábricas.
 */
public interface Toast {
    void display();
}
