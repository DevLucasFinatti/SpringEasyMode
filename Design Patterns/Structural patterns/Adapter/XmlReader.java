package adapter;

/**
 * Classe existente (Adaptee).
 * Tem uma interface diferente que queremos adaptar.
 */
public class XmlReader {
    public String readXml() {
        return "<data>Conteúdo XML</data>";
    }
}
